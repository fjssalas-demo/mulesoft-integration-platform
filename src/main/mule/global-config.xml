<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:doc="http://www.mulesoft.org/schema/mule/documentation"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:db="http://www.mulesoft.org/schema/mule/db"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/current/mule-http.xsd
        http://www.mulesoft.org/schema/mule/db http://www.mulesoft.org/schema/mule/db/current/mule-db.xsd">

    <configuration-properties file="config-${mule.env}.properties" doc:name="Configuration properties"/>

    <http:listener-config name="HTTP_Listener_Config" doc:name="HTTP Listener Config">
        <http:listener-connection host="0.0.0.0" port="${http.port}"/>
    </http:listener-config>

    <db:config name="Database_Config" doc:name="Database Config">
        <db:generic-connection url="${db.url}" driverClassName="${db.driver}"
                               user="${db.username}" password="${db.password}"/>
    </db:config>

    <!-- Global error handler -->
    <error-handler name="global-error-handler">
        <on-error-propagate type="HTTP:UNAUTHORIZED">
            <set-payload value='#[output application/json --- { error: "Unauthorized", status: 401 }]'/>
        </on-error-propagate>
        <on-error-propagate type="HTTP:NOT_FOUND">
            <set-payload value='#[output application/json --- { error: "Not Found", status: 404 }]'/>
        </on-error-propagate>
        <on-error-propagate type="ANY">
            <logger level="ERROR" message="#[error.description]" doc:name="Log Error"/>
            <set-payload value='#[output application/json --- { error: "Internal Server Error", status: 500, detail: error.description }]'/>
        </on-error-propagate>
    </error-handler>

</mule>
